language: fr        # "fr" (French) or "en" (English)

llm:
  provider: ollama
  model: qwen3
  host: http://localhost:11434

tts:
  engine: gtts

anki:
  deck_name: ""          # Leave empty to use language profile default
  package_name: ""       # Leave empty to use language profile default

paths:
  input: "input/vocab.txt"
  output_dir: "output"
  cache: "cache"
